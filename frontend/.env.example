# Frontend API URL
# Note: When using Docker Compose, environment variables are read from the root .env file, not this file.
# This file is only used when running the frontend manually (npm run dev) without Docker.
VITE_API_URL=http://localhost:3000
